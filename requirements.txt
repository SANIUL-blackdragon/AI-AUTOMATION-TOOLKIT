# Core Python dependencies for AI-AUTOMATION-TOOLKIT skills
# Generated based on comprehensive codebase scan

# XML/OOXML Processing (docx, pptx)
defusedxml
lxml
python-pptx
markitdown[pptx]  # Used via CLI for text extraction
six

# PDF Processing
pypdf
pdf2image

# Spreadsheet Processing (xlsx)
openpyxl

# Web Automation
playwright

# Image Processing
Pillow

# Skill Management & Configuration
PyYAML

# System-level dependencies (Pre-requisites):
# These cannot be installed via pip and must be installed on the system:
# - LibreOffice (Required for PDF conversion and Excel formula recalculation)
#   - Linux: sudo apt-get install libreoffice
#   - Mac: brew install libreoffice
# - Poppler (Required for pdf2image)
#   - Linux: sudo apt-get install poppler-utils
#   - Mac: brew install poppler
# - Tesseract OCR (Optional, for OCR capabilities in some scripts)
